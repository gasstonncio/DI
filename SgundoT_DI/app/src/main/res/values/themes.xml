<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Modo Claro) -->
    <style name="Base.Theme.SgundoT_DI" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/montserrat</item>

    </style>

    <!-- Tema principal que hereda de la base -->
    <style name="Theme.SgundoT_DI" parent="Base.Theme.SgundoT_DI"/>

    <!-- Tema para la SplashScreen -->
    <style name="Theme.SplashScreen" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@color/soft_purple</item>
    </style>
</resources>
