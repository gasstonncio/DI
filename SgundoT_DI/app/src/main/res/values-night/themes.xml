<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Modo Oscuro) -->
    <style name="Base.Theme.SgundoT_DI" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/montserrat</item>

    </style>

    <!-- Tema principal en modo oscuro -->
    <style name="Theme.SgundoT_DI" parent="Base.Theme.SgundoT_DI"/>

    <!-- Tema para la SplashScreen en modo oscuro -->
    <style name="Theme.SplashScreen" parent="Theme.Material3.Dark.NoActionBar">

    <item name="android:windowBackground">@color/soft_purple</item>
    </style>
</resources>
